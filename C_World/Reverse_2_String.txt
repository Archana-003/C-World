Reverse the two string without using any predefined function.

Conditions:
The two string should be get in one variable.

#include stdio.h
#include string.h

int main() {
    int count =0,i=0;
    char c[2][100];
    char t[100]=Archu mani;
    int a=0,temp=0;
    int len = strlen(t);
    while(ilen)
    {
        if(t[i]==' ')
        {
            count=i;
            break;
        }
        i++;
    }
    int f=count-1;
    
    for(i=0;i2;i++)
    {
        temp=0;
        for(int j=f;j=a;j--)
        {
            c[i][temp]=t[j];
            printf(%c,c[i][temp]);
            temp++;
            if(j==0)
            {
                f=(len-1);
                a=count;
            }
        }
    }
    printf(%s%s,c[0],c[1]);
    return 0;
}